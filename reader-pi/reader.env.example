# Copy to reader.env (chmod 600) on the Pi. READER_TOKEN must match the
# READER_TOKEN set in Netlify env. Generate one with: openssl rand -hex 32
READER_TOKEN=replace-with-a-long-random-hex-string
PROFILE_DIR=/home/pi/accord-reader/profile
CHROMIUM_PATH=/usr/bin/chromium
PORT=8787
