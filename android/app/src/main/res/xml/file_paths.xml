<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- AppUpdater downloads the APK to getExternalFilesDir(DIRECTORY_DOWNLOADS),
         i.e. <external-files>/Download/. Exposed to the package installer via
         FileProvider (authority ${applicationId}.fileprovider). -->
    <external-files-path name="apk_download" path="Download/" />
</paths>
