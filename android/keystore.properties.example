# Copy to android/keystore.properties (git-ignored) and fill in.
# The keystore itself lives OUTSIDE the repo — back it up; losing it means
# you can never ship an update to the same app again.
storeFile=C:/Users/you/.android/accord-release.jks
storePassword=
keyAlias=accord
keyPassword=
